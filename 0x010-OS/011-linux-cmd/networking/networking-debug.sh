# basic command to debug networking in linux 
#   - ping
#   - traceroute # traceroute is a computer network diagnostic tool for displaying the route (path) and measuring transit delays of packets across an Internet Protocol (IP) network
#   - netstat # netstat is a command-line network utility which displays network connections for Transmission Control Protocol (TCP), User Datagram Protocol (UDP), and Transmission Control Protocol/Internet Protocol (TCP/IP) network protocols
netstat -tulpn # -t: TCP, -u: UDP, -l: listening, -p: PID, -n: numeric
#   - tcpdump
#   - nmap # nmap is a free and open source utility for network discovery and security auditing
arp -a              
# arp is a utility for displaying, modifying, and/or deleting the system's Address Resolution Protocol (ARP) cache
#   - dig # dig is a network administration command-line tool for querying Domain Name System (DNS) name servers

#   - nslookup # nslookup is a computer network administration command-line tool for querying the Domain Name System (DNS) to obtain domain name or IP address mapping or for any other specific DNS record
#   - host
#   - wget
#   - curl
#   - telnet  # telnet is a protocol used on the Internet or local area networks to provide a bidirectional interactive text-oriented communication facility using a virtual terminal connection

#   - ssh
#   - scp
#   - rsync
#   - ftp
#   - sftp
#   - lftp

#   - nc (netcat)   # netcat is a utility for reading from and writing to network connections using TCP or UDP
#   - socat  # socat is a command line based utility that establishes two bidirectional byte streams and transfers data between them
#   - netcat (nc)   # netcat is a utility for reading from and writing to network connections using TCP or UDP

#   - netcat-openbsd
#   - netcat-traditional
#   - netcat-ubuntu
#   - netcat-gnome
#   - netcat-ncat
#   - netcat-nc
#   - netcat-openbsd
#   - netcat-traditional
#   - netcat-ubuntu

